# <需求编号或功能名> 测试报告工作稿

> 本文档在执行测试前先根据测试实施指南生成骨架；执行过程中每完成一个 case，立即更新对应执行明细。全部可执行 case 处理完后，再从本文档提炼最终摘要或团队报告。

## 1. 执行摘要

| 字段 | 内容 |
| --- | --- |
| 执行人 | `<name>` |
| 执行日期 | `<YYYY-MM-DD>` |
| 环境 | `<env / host>` |
| 需求 / 版本 | `<issue / package / commit / branch>` |
| 测试用例 | `<path>` |
| 测试实施指南 | `<path>` |
| 执行工具 | `<tool list>` |
| 执行结论 | `<待执行 / 通过数 / 失败数 / 需复测 / 部分覆盖 / 不执行 / 阻塞>` |
| 恢复结果 | `<已恢复 / 未恢复 / 不适用>` |

## 1.1 当前执行口径说明

| 项目 | 口径 |
| --- | --- |
| 本轮覆盖范围 | `<说明>` |
| 部分覆盖范围 | `<说明>` |
| 本轮不执行项 | `<说明>` |
| 外部依赖限制 | `<说明>` |
| 人工已完成项 | `<说明>` |

## 2. 测试工具选型结果

| 测试工具 | 适用范围 | 当前能力判断 | 是否需要改造 | 改造方向 / 验收标准 | 人类确认状态 | 实际使用结果 |
| --- | --- | --- | --- | --- | --- | --- |
| 自动化用例工具 | `<精确单 case / 固定回归 / 自动断言等>` | `<支持 / 部分支持 / 不支持>` | `<否 / 是 / 待确认>` | `<字段、用例、断言、报告字段、分析脚本等>` | `<已确认 / 待确认 / 不适用>` | `<ok / failed / blocked>` |
| 长稳 / 持续测试工具 | `<跨周期 / 长跑 / 持续流量 / 状态保留等>` | `<支持 / 部分支持 / 不支持>` | `<否 / 是 / 待确认>` | `<样本生成、参数、跨周期状态、证据采集、结果分析等>` | `<已确认 / 待确认 / 不适用>` | `<ok / failed / blocked>` |
| 依赖模拟器 / mock 服务 | `<外部特殊回报 / 下游语义等>` | `<支持 / 部分支持 / 不支持>` | `<否 / 是 / 待确认>` | `<正常应答/拒绝、部分/完全完成、超时、边界事件等>` | `<已确认 / 待确认 / 不适用>` | `<ok / failed / blocked>` |

## 3. 执行记录明细

> 按 Case ID 排序。若实际执行顺序不同，可在每个 case 中记录实际执行时间。

### <Case ID> <Case Name>

| 字段 | 内容 |
| --- | --- |
| 状态 | `<待执行 / 通过 / 失败 / 需复测 / 当前口径部分覆盖 / 本轮不执行 / 不单独执行但已覆盖 / 阻塞>` |
| 执行时间 | `<timestamp>` |
| 前置条件 | `<实际前置状态>` |
| 测试环境 | `<test / test01 / uat>` |
| 测试工具 | `<自动化用例工具 / 长稳工具 / mock 服务，如多工具则列全>` |
| 自动化用例来源 | `<不适用 / 已有 case / proposed_cases.<ext> / 落地文件>` |
| 自动化草稿审核 | `<不适用 / 待审核 / 已确认 / 需修改>` |
| 自动化落地对账 | `<不适用 / 未落地 / 待对账 / 通过 / 失败；记录对账命令和摘要，diff 明细放 .agent_tmp 不提交>` |
| 输入 | `<request / data / config / env / state>` |
| 环境切换记录 | `<before -> after，如不适用写不适用>` |
| 关键标识 | `<业务单号 / request id / trace id / account / dataset>` |
| 实际结果 | `<observed result>` |
| 断言结果 | `<assert pass/fail>` |
| 证据 | `<log / report / DB / screenshot / monitoring path；自动化已断言 case 记录工具 report/log/用例定义/关键业务标识即可>` |
| 结论 | `<case conclusion>` |
| 备注 | `<限制 / 风险 / 后续动作>` |

长周期 / 多轮观察 case 追加以下结构；普通 case 可省略：

| 字段 | 内容 |
| --- | --- |
| 执行周期 | `<单次 / 跨日 / 多天 / 长跑>` |
| 启动时间 | `<timestamp>` |
| 预计完成时间 | `<timestamp / 业务日 / 观察窗口>` |
| 可续跑状态 | `<已写入输入 / 已写入关键标识 / 待观察 / 观察中 / 已完成 / 阻塞>` |
| 下一轮检查时间 | `<timestamp / 触发条件>` |
| 下一轮检查动作 | `<下一位 agent 需要执行的检查>` |

输入与关键标识：

| 类型 | 内容 | 证据 / 来源 |
| --- | --- | --- |
| 请求参数 | `<请求类型 / 关键字段 / 数量 / 账户 / 会话等>` | `<命令 / 配置 / fixture / report>` |
| 初始回执 | `<ack / reject / pending / not yet observed>` | `<log / report path>` |
| 关键 ID | `<request id / 业务单号 / trace id / dataset>` | `<来源>` |

观察节点：

| 节点 | 计划检查时间 / 触发条件 | 应检查断言 | 证据要求 | 当前状态 | 实际观察 / 证据 |
| --- | --- | --- | --- | --- | --- |
| `<T0 提交后>` | `<timestamp / 条件>` | `<断言>` | `<证据路径或类型>` | `<待观察 / 已完成 / 阻塞>` | `<实际结果>` |
| `<T1 跨日后>` | `<timestamp / 条件>` | `<断言>` | `<证据路径或类型>` | `<待观察 / 已完成 / 阻塞>` | `<实际结果>` |

## 4. 恢复记录（如适用）

若本轮未切换包、配置、启动参数、服务状态、测试流量或其它环境配置，本节填写“不适用”。

| 字段 | 内容 |
| --- | --- |
| 恢复目标 | `<original state>` |
| 恢复方式 | `<operation>` |
| 恢复前状态 | `<version / config / PID / health>` |
| 恢复后状态 | `<version / config / PID / health>` |
| 恢复校验 | `<evidence>` |
| 恢复结果 | `<通过 / 失败 / 不适用>` |

## 5. 覆盖矩阵

| Case ID | 状态 | 本轮执行记录 / 证据摘要 |
| --- | --- | --- |
| `<Case ID>` | `<status>` | `<short evidence>` |

状态枚举：

- `待执行`
- `通过`
- `失败`
- `需复测`
- `当前口径部分覆盖`
- `本轮不执行`
- `不单独执行但已覆盖`
- `阻塞`

## 6. 补充执行明细

| 项目 | 记录 | 证据 |
| --- | --- | --- |
| `<工具新增>` | `<新增 case / 脚本 / 配置>` | `<path>` |
| `<环境切换>` | `<before -> after>` | `<evidence>` |
| `<特殊观察>` | `<observation>` | `<evidence>` |

## 7. 未覆盖 / 阻塞项

| Case / 场景 | 状态 | 原因 | 后续建议 |
| --- | --- | --- | --- |
| `<Case ID>` | `<本轮不执行 / 阻塞 / 需复测>` | `<原因>` | `<next action>` |

## 8. 测试工程反思候选

| 类型 | 内容 |
| --- | --- |
| 规则缺口 | `<本次暴露的规则缺口>` |
| skill / 流程缺口 | `<本次暴露的 skill 或流程缺口>` |
| 工具缺口 | `<本次暴露的工具或自动化缺口>` |
| 建议沉淀项 | `<AGENTS / 字典 / skill / checklist / tool>` |
